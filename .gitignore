bin/
obj/

# файлы, которые программа создаёт рядом с собой при запуске
config.json
log.txt

# готовый портабельный exe нужен в репозитории, спутники сборки — нет
release/*
!release/AudioDirigent.exe

# служебные файлы Visual Studio: держатся открытыми, git на них падает
.vs/
*.user
